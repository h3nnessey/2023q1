import { getUrl } from '../utils';
import { PATHS } from '../constants';

export class WinnersService {
  public static async getWinners() {
    // const response: Response = await fetch(getUrl(`${PATHS.WINNERS}`));
  }
}
